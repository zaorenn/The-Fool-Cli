# Changelog

`foolrs` is a fork of [aionrs](https://github.com/iOfficeAI/aionrs) (Apache-2.0), vendored into this repository at version 0.2.8. Release history from before the fork lives in that project; this file records what has changed here.

## 0.2.8 — unreleased

- Crates renamed to the `foolrs-*` namespace and consumed by path from `backend/core`, so the agent SDK is built from source here instead of resolved from a git tag.
