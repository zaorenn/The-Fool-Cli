name: Background Notarization

# 当主构建完成后自动触发 / Triggered automatically after main build completes
on:
  workflow_run:
    workflows: ["Build and Release"]
    types: [completed]
    branches: [main, dev]

  # 支持手动触发重新公证 / Support manual re-notarization
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Build workflow run ID to notarize (leave empty to use latest)'
        required: false
        type: string

jobs:
  notarize:
    # 只在主构建成功或手动触发时运行 / Only run if main build succeeded or manually triggered
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    runs-on: macos-latest
    timeout-minutes: 120  # 增加到 120 分钟 / Increased to 120 minutes for Apple delays

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Determine workflow run ID
        id: run_id
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # 手动触发：使用输入的 run_id 或查找最新的成功构建
            if [ -n "${{ inputs.run_id }}" ]; then
              RUN_ID="${{ inputs.run_id }}"
              echo "📝 Using manually specified run ID: $RUN_ID"
            else
              RUN_ID=$(gh run list --workflow "Build and Release" --status success --limit 1 --json databaseId --jq '.[0].databaseId')
              echo "🔍 Using latest successful build run ID: $RUN_ID"
            fi
          else
            # 自动触发：使用触发此 workflow 的 run_id
            RUN_ID="${{ github.event.workflow_run.id }}"
            echo "🤖 Using triggering workflow run ID: $RUN_ID"
          fi

          echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download artifacts from main build
        uses: actions/download-artifact@v4
        with:
          name: macos-build
          path: ./artifacts
          run-id: ${{ steps.run_id.outputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if notarization is needed
        id: check
        run: |
          if [ -f ./artifacts/signing-info.json ]; then
            echo "notarization_needed=true" >> $GITHUB_OUTPUT
            echo "✅ Found signing-info.json, will proceed with notarization"
          else
            echo "❌ No signing-info.json found - macOS build likely failed"
            echo "This means the macOS build artifact was not uploaded properly"
            exit 1
          fi

      - name: Extract signing info
        if: steps.check.outputs.notarization_needed == 'true'
        id: info
        run: |
          APP_NAME=$(node -p "require('./artifacts/signing-info.json').appName")
          echo "app_name=$APP_NAME" >> $GITHUB_OUTPUT
          echo "📦 App name: $APP_NAME"

      - name: Find DMG file
        if: steps.check.outputs.notarization_needed == 'true'
        id: dmg
        run: |
          # 从 signing-info.json 获取 appName / Get appName from signing-info.json
          APP_NAME=$(node -p "require('./artifacts/signing-info.json').appName")

          # 使用 appName 精确匹配 DMG / Use appName to match DMG precisely
          DMG_FILE=$(ls ./artifacts/${APP_NAME}*.dmg 2>/dev/null | head -1)

          if [ -z "$DMG_FILE" ]; then
            echo "❌ No DMG file found matching pattern: ${APP_NAME}*.dmg"
            echo "Available files in artifacts:"
            ls -la ./artifacts/
            exit 1
          fi

          DMG_COUNT=$(ls ./artifacts/${APP_NAME}*.dmg 2>/dev/null | wc -l)
          if [ "$DMG_COUNT" -gt 1 ]; then
            echo "⚠️  Warning: Multiple DMG files found, using first one:"
            ls ./artifacts/${APP_NAME}*.dmg
          fi

          echo "dmg_path=$DMG_FILE" >> $GITHUB_OUTPUT
          echo "✅ Found DMG: $DMG_FILE"

      - name: Extract and compress app for notarization
        if: steps.check.outputs.notarization_needed == 'true'
        id: compress
        run: |
          DMG_PATH="${{ steps.dmg.outputs.dmg_path }}"
          APP_NAME=$(node -p "require('./artifacts/signing-info.json').appName")

          echo "📦 Mounting DMG..."
          MOUNT_POINT=$(hdiutil attach "$DMG_PATH" -nobrowse -readonly | tail -1 | awk '{print $3}')

          if [ -z "$MOUNT_POINT" ]; then
            echo "❌ Failed to mount DMG"
            exit 1
          fi

          echo "✅ DMG mounted at: $MOUNT_POINT"

          # 查找 .app 文件
          APP_PATH=$(find "$MOUNT_POINT" -name "${APP_NAME}.app" -maxdepth 2 | head -1)

          if [ -z "$APP_PATH" ]; then
            echo "❌ .app not found in DMG"
            hdiutil detach "$MOUNT_POINT" -force || true
            exit 1
          fi

          echo "✅ Found app: $APP_PATH"

          # 使用 ditto 压缩（Apple 推荐方式）
          ZIP_PATH="./artifacts/${APP_NAME}.zip"
          echo "📦 Compressing app to ZIP..."
          ditto -c -k --keepParent "$APP_PATH" "$ZIP_PATH"

          # 卸载 DMG
          hdiutil detach "$MOUNT_POINT" -force || true

          # 检查 ZIP 大小
          DMG_SIZE=$(ls -lh "$DMG_PATH" | awk '{print $5}')
          ZIP_SIZE=$(ls -lh "$ZIP_PATH" | awk '{print $5}')

          echo "✅ Compression complete!"
          echo "   DMG size: $DMG_SIZE"
          echo "   ZIP size: $ZIP_SIZE"

          echo "zip_path=$ZIP_PATH" >> $GITHUB_OUTPUT

      - name: Submit for notarization with auto-retry
        if: steps.check.outputs.notarization_needed == 'true'
        id: submit
        run: |
          ZIP_PATH="${{ steps.compress.outputs.zip_path }}"
          MAX_RETRIES=3
          RETRY_DELAY=300  # 5分钟 / 5 minutes

          for attempt in $(seq 1 $MAX_RETRIES); do
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🚀 Notarization attempt $attempt/$MAX_RETRIES"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "📦 Submitting ZIP: $ZIP_PATH"

            # 优先使用 API Key / Prefer API Key if available
            if [ -n "${{ secrets.APPLE_API_KEY }}" ]; then
              echo "🔑 Using API Key authentication"
              echo "${{ secrets.APPLE_API_KEY }}" | base64 -d > /tmp/authkey.p8

              RESULT=$(xcrun notarytool submit "$ZIP_PATH" \
                --key /tmp/authkey.p8 \
                --key-id "${{ secrets.APPLE_API_KEY_ID }}" \
                --issuer "${{ secrets.APPLE_API_ISSUER }}" \
                --wait \
                --timeout 60m \
                --output-format json 2>&1) || SUBMIT_FAILED=true

              rm -f /tmp/authkey.p8
            else
              echo "🔑 Using Apple ID authentication"

              RESULT=$(xcrun notarytool submit "$ZIP_PATH" \
                --apple-id "${{ secrets.APPLE_ID }}" \
                --password "${{ secrets.APPLE_ID_PASSWORD }}" \
                --team-id "${{ secrets.TEAM_ID }}" \
                --wait \
                --timeout 60m \
                --output-format json 2>&1) || SUBMIT_FAILED=true
            fi

            echo "$RESULT"

            # 检查是否成功 / Check if successful
            if [ -z "$SUBMIT_FAILED" ]; then
              SUBMISSION_ID=$(echo "$RESULT" | grep -o '"id": *"[^"]*"' | head -1 | grep -o '"[^"]*"$' | tr -d '"')
              STATUS=$(echo "$RESULT" | grep -o '"status": *"[^"]*"' | head -1 | grep -o '"[^"]*"$' | tr -d '"')

              if [ "$STATUS" = "Accepted" ]; then
                echo "✅ Notarization successful on attempt $attempt!"
                echo "submission_id=$SUBMISSION_ID" >> $GITHUB_OUTPUT
                echo "status=$STATUS" >> $GITHUB_OUTPUT
                exit 0
              else
                echo "⚠️  Notarization returned status: $STATUS"
              fi
            else
              echo "⚠️  Notarization submission failed or timed out"
            fi

            # 如果不是最后一次尝试，等待后重试 / If not last attempt, wait and retry
            if [ $attempt -lt $MAX_RETRIES ]; then
              echo "⏳ Waiting $RETRY_DELAY seconds before retry..."
              sleep $RETRY_DELAY
            fi

            unset SUBMIT_FAILED
          done

          # 所有尝试都失败 / All attempts failed
          echo ""
          echo "❌ Notarization failed after $MAX_RETRIES attempts"
          echo "Last result: $RESULT"
          exit 1

      - name: Staple notarization ticket
        if: steps.check.outputs.notarization_needed == 'true' && steps.submit.outputs.status == 'Accepted'
        run: |
          echo "📎 Stapling notarization ticket..."
          xcrun stapler staple "${{ steps.dmg.outputs.dmg_path }}"
          echo "✅ Stapling complete!"

      - name: Verify notarization
        if: steps.check.outputs.notarization_needed == 'true' && steps.submit.outputs.status == 'Accepted'
        run: |
          echo "🔍 Verifying notarization..."
          xcrun stapler validate "${{ steps.dmg.outputs.dmg_path }}"
          spctl -a -vvv -t install "${{ steps.dmg.outputs.dmg_path }}"
          echo "✅ Verification successful!"

      - name: Get release info
        if: steps.check.outputs.notarization_needed == 'true' && steps.submit.outputs.status == 'Accepted'
        id: release
        run: |
          # 从 signing-info.json 读取 release tag / Read release tag from signing-info.json
          RELEASE_TAG=$(node -p "require('./artifacts/signing-info.json').releaseTag || ''")

          if [ -z "$RELEASE_TAG" ]; then
            echo "❌ No release tag found in signing-info.json"
            echo "This means the release job did not update signing-info.json properly"
            echo "Artifact contents:"
            ls -la ./artifacts/
            cat ./artifacts/signing-info.json
            exit 1
          fi

          echo "release_tag=$RELEASE_TAG" >> $GITHUB_OUTPUT
          echo "✅ Target release: $RELEASE_TAG"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload notarized DMG to release
        if: steps.check.outputs.notarization_needed == 'true' && steps.submit.outputs.status == 'Accepted'
        run: |
          DMG_NAME=$(basename "${{ steps.dmg.outputs.dmg_path }}")

          echo "🚀 Uploading notarized DMG to release..."

          # 删除旧的 DMG（如果存在）/ Delete old DMG if exists
          gh release delete-asset "${{ steps.release.outputs.release_tag }}" "$DMG_NAME" --yes || true

          # 上传新的公证过的 DMG / Upload new notarized DMG
          gh release upload "${{ steps.release.outputs.release_tag }}" "${{ steps.dmg.outputs.dmg_path }}" --clobber

          echo "✅ Notarized DMG uploaded successfully!"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Add notarization success comment
        if: steps.check.outputs.notarization_needed == 'true' && steps.submit.outputs.status == 'Accepted'
        run: |
          gh release edit "${{ steps.release.outputs.release_tag }}" --notes-file - <<EOF
          $(gh release view "${{ steps.release.outputs.release_tag }}" --json body --jq '.body')

          ---

          ✅ **macOS App Notarized** ($(date -u +"%Y-%m-%d %H:%M:%S UTC"))

          The macOS DMG has been successfully notarized by Apple and is now fully verified for distribution.
          EOF
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Background notarization failed"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Current Status:"
          echo "   • Signed DMG is available in the release"
          echo "   • Users can install by right-clicking and selecting 'Open'"
          echo "   • Notarization failed - DMG is not fully verified"
          echo ""
          echo "🔄 How to Retry:"
          echo "   1. Go to Actions → Background Notarization"
          echo "   2. Click 'Run workflow'"
          echo "   3. Leave run_id empty to retry latest build"
          echo "   4. Or specify the build run ID"
          echo ""
          echo "🕐 Typical Causes:"
          echo "   • Apple notarization service timeout (> 120 min)"
          echo "   • Apple service temporarily unavailable"
          echo "   • Network connectivity issues"
          echo ""
          echo "💡 Workflow URL:"
          echo "   https://github.com/${{ github.repository }}/actions/workflows/notarize-background.yml"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
